import logo from './logo.svg';
import react from "react";
import './App.css';
import basicInfo from './components/basicInfo';
import Loading from './components/Loading';
import Employees from './components/totalinfo';
import { useEffect, useState } from 'react';
import lscache from 'lscache';
import axios from 'axios';
import { getByAltText } from '@testing-library/react';
import employees from '/node_modules/inputinfo.js';
import employee from '/node_modules/inputinfo1.js';





const basicInfo = (props) => {
 function header (

 )

 <div class="card text-dark bg-light mb-3" style="max-width: 18rem;">
  <div class="card-header">Employee Data</div>
  <div class="card-body">{
     return const =>basicInfo}
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
  </div>
  const [inputValue,  setInputValue] = useState('');


  
  return props.employees.map{(employees =>{
      console.log(employee);
      key={employee.id}
      onclick{() => (employees(employee.id))} role="button">
  });
}
;

const totalInfo = (props) => {


  return props.employeesTotal.map{(employee =>{
      console.log(employees);
      key={employee.id};
  });
}};

  const Employee.id = (employee.id) => {
}

props.employees.map{(employees =>{
      key={employee.Id}
      onclick{() => (employeeTotal(employee.id))} role="button">
  });
}
;
        const employeePhoto = {
            Employee.Id (employeeTotal.photo)
        }

<div class="col">
    <div class="card h-100">
      <img src = employeePhoto class="card-img-top" alt="...">

      <div class="card-body">
        <h5 class="card-title">Employee Selection</h5>
          <div class = employeeTotal ><div>
      </div>
      <div class="card-footer">
        <small class="text-muted"></small>
      </div>
    </div>
  </div>
</div>

export default function (togo) {
  const [loading, setLoading] = useState (true);
  const [totalInfo, setTotalInfo] = useData ([]);
  const [basicInfo, setBasicInfo] = useState(null);
  const props;
  const lscacheKey = 'id';
}
export {default header};
export {employeeID};