import "./src/app.js";
import employeesTotal from '/nodemodules/inputinfo1.js'
import employee.ID "./App.css";
import  "./src/inputinfo1.js";
import  "./src/inputinfo.js";
import employees.map  from "/basicInfo.js"

const employeesTotal = (props) => {
    props.setEmployee(employee);
};

return {
    <div class = 'card-body'>
  <div class = 'card-header'>
}
   import employeesTotal from 'node_modules/loading1.js';

    
    const totalInfo = (props) => {
    
        const [inputValue,  setInputValue] = useState('');
    
        return props.employees.map{(employees =>{
            console.log(employees);
            key={specificEmployee.id}
 
            <p></p> {employees.id}
            <p></p> {employees.name}
            <p></p> {employees.department}
            <p></p> {employees.role}  
            <p></p> {employees.photo}  
            <p></p> {employees.startdate}            
        });
    }}
     
    
    
    export default totalInfo;
    

 const updateEmployee = (employeeId) =>{
    props.setEmployee(employeeId);

};

    return (
    <div style>{

    props.data.map(employee => {
        return {
            <p 
            key={employee.id}
            onclick {() => (updateEmployee(employee.id) )}  role="button"><h1>Employee Information</h1></h1>{employee.name}<p></p>{employee.id} <p></p> {employee.department}<p></p><p></p> {employee.startdate}<p></p> {employee.role}<p></p> {employee.department}<p></p>{employee.photo}<p></p>;
    </p>}}
    );
        };

</div>;
  
    )
  
</div>
export default Employee;